apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: postgrest
  namespace: cmp-dev-1
  annotations:
    kubernetes.io/ingress.class: cloudflare-tunnel
    cloudflare-tunnel-ingress-controller.strrl.dev/backend-protocol: http
spec:
  rules:
    - host: pgrest-k8s-argocd-au.samuka007.com
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: postgrest
                port:
                  name: http